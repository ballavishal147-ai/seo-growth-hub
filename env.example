# Copy to .env.local for local development. Never commit real values.

# --- Public (exposed to the browser, must be prefixed) ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# --- Server-only (Phase 5A/5B will define the real integrations) ---
# DATABASE_URL=
# AUTH_SECRET=
